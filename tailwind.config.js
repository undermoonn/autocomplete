const { defineConfig } = require('vite-plugin-windicss')

/**
 * for vscode Tailwind CSS IntelliSense
 * add tailwindcss package as devDependencies
 */
module.exports = defineConfig({
  darkMode: false,
  theme: {},
  variants: {},
  plugins: []
})
